{
  "name": "openlectern",
  "private": true,
  "version": "1.0.0",
  "description": "OpenLectern app + session API/realtime on Cloudflare Workers + Durable Objects",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "type": "module"
}
